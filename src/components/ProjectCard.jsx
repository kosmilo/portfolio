import React from "react";

const ProjectCard = ({project}) => {
    return (
        <section id="projects">
            <h3>{project.title}</h3>
        </section>
    )
}

export default ProjectCard;